# textstats

A small text statistics library: word counts, character counts, average
word length, and longest word.

## Running the tests

The suite uses only the Python standard library (`unittest`). From this
directory:

```text
python -m unittest discover -v
```

All tests should pass. If any test fails, treat the test suite as the
source of truth for the intended behavior.
